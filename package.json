{
  "name": "kite-security-command-center",
  "version": "0.1.0",
  "private": true,
  "description": "Security and risk command center for Kite agent wallets and contracts.",
  "type": "module",
  "packageManager": "pnpm@9.15.9",
  "scripts": {
    "dev": "pnpm --parallel --filter @kite-security-command-center/api --filter @kite-security-command-center/web dev",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "@types/node": "^24.10.2",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "esbuild": "^0.28.1"
  },
  "dependencies": {
    "@hono/node-server": "^1.19.6",
    "hono": "^4.10.7"
  }
}
