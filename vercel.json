{
  "installCommand": "pnpm install --frozen-lockfile=false",
  "buildCommand": "pnpm --filter @kite-security-command-center/web build",
  "outputDirectory": "packages/web/dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/"
    }
  ]
}
